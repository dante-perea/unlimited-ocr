# Pytest configuration for the backend.
#
# Run from backend/:   .venv/bin/pytest -q
#
# conftest.py forces OCR_MOCK=1 for the whole suite (and the model loader is
# monkeypatched where needed) so no torch/transformers/weights are required.

[pytest]
testpaths = tests
addopts = -ra
