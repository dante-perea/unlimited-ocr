# Copy to frontend/.env.local and adjust if your backend runs elsewhere.
#
# NEXT_PUBLIC_* values are inlined into the browser bundle at build time, so the
# backend URL must be reachable from the user's browser. Defaults to the local
# FastAPI dev server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
